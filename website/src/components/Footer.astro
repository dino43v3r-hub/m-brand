---
const base = import.meta.env.BASE_URL;
const basePath = base.endsWith('/') ? base.slice(0, -1) : base;
const withBase = (path: string) => `${basePath}${path === '/' ? '/' : path}`;

const footerLinks = [
  { href: '/', label: 'Home' },
  { href: '/about', label: 'About' },
  { href: '/connectaloud', label: 'Articles' },
  { href: '/projects', label: 'Projects' },
  { href: '/resources', label: 'Resources' },
  { href: '/connect', label: 'Connect' },
];
---

<footer class="site-footer">
  <div class="footer-statement">
    <p class="footer-brand">ConnectAloud</p>
    <p>Thoughtful conversations exploring AI, technology, leadership, and theology.</p>
  </div>
  <nav class="footer-nav" aria-label="Footer navigation">
    {
      footerLinks.map((link) => (
        <a href={withBase(link.href)}>{link.label}</a>
      ))
    }
  </nav>
  <div class="footer-signoff">
    <p>&copy; 2026 ConnectAloud</p>
    <p>Built with curiosity.</p>
  </div>
</footer>
