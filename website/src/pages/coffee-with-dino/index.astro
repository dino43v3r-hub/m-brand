---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Section from '../../components/Section.astro';
import { articles } from '../../data/articles';

const pageTitle = 'Saturday Morning Coffee with Dino';
const description =
  'Coffee with Dino is an occasional ConnectAloud conversation about AI, leadership, technology, faith, and what it means to be human.';
const base = import.meta.env.BASE_URL;
const basePath = base.endsWith('/') ? base.slice(0, -1) : base;
const withBase = (path: string) => `${basePath}${path}`;

const conversations = articles.filter((article) => article.collection === pageTitle);
---

<BaseLayout
  title={`${pageTitle} | ConnectAloud`}
  description={description}
  type="website"
  canonicalPath="/coffee-with-dino/"
  ogImage="/images/dino/dino-coffee-v1.0.jpg"
  author="M"
  keywords="Coffee with Dino, AI, leadership, technology, faith, human identity"
>
  <section class="coffee-landing-hero" aria-labelledby="page-title">
    <p class="coffee-mark" aria-hidden="true">☕</p>
    <h1 id="page-title">{pageTitle}</h1>
    <div class="coffee-landing-copy">
      <p>Sometimes the best conversations don't begin with answers.</p>
      <p>They begin with someone willing to say,</p>
      <p>"I don't know."</p>
      <p>
        Coffee with Dino is an occasional conversation about AI, leadership, technology, faith, and what it means to be human.
      </p>
      <p>Pull up a chair, pour yourself a cup of coffee, and think alongside us.</p>
    </div>
  </section>

  <Section id="conversations" title="Pull Up a Chair">
    <div class="section-intro coffee-section-intro">
      <p>Every Coffee with Dino conversation begins with curiosity.</p>
      <p>Pull up a chair, pour yourself a cup of coffee, and think alongside us.</p>
    </div>
    <div class="coffee-conversation-grid">
      {
        conversations.map((conversation) => (
          <a class="article-card coffee-conversation-card" href={withBase(conversation.href)}>
            <div class="article-card-meta">
              <span>{conversation.displayDate} · {conversation.readingTime}</span>
            </div>
            <h3>{conversation.title}</h3>
            <p>{conversation.description}</p>
          </a>
        ))
      }
    </div>
  </Section>
</BaseLayout>
